///|
/// MoonJS CLI entry point.
/// Milestone M1: prints a banner and exits 0.
/// Milestone M5: implements `run <file>`, `-e <code>`, and QuickJS-compatible argv parsing.
fn main {
  println("moonjs (M1 scaffold)")
}
