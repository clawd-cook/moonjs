---
title: API Reference
description: MoonJS public API — Engine, eval_script, JSValue, JSException.
overview: true
---

MoonJS exposes a small public API from the root package `heyq02/moonjs`. Everything else lives in `src/*` subpackages and is imported only when embedding-time customization requires it.
