// Value package: JSValue enum, Object shape, property descriptor.
// Owner milestone: M1 (primitives + Object base). New variants added by later milestones (Sym in M3, BigInt in M4) must be additive.
