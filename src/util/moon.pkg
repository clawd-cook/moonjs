// Util package: shared helpers (SourceLoc, SourceSpan, Interner) used across
// the MoonJS pipeline. Kept minimal and dependency-free apart from stdlib.
// Owner milestone: M1.

import {
  "moonbitlang/core/debug",
  "moonbitlang/core/hashmap",
}

import {
  "moonbitlang/core/test",
} for "test"
